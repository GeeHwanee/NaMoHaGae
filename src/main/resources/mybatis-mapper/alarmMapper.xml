<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kro.namohagae.member.dao.AlarmDao">
    <insert id="save" parameterType="alarm">
    insert into ALARM(alarm_no,member_no,alarm_content,alarm_rink,alarm_read_enabled) values (#{alarmNo},#{memberNo},#{alarmContent},#{alarmRink},#{alarmReadEnabled})
    </insert>

    <select id="count" resultType="int">
        select count(bno) from alarm
    </select>

    <select id="findAll" resultType="String">
        select * from
            (
                select rownum as rnum, a.* from
                    (
                        select
                            alarm_no,alarm_content,alarm_rink,alarm_read_enabled
                        from alarm
                    ) a
                where rownum&lt;=#{endRownum}
            )
        where rnum&gt;=#{startRownum}
    </select>
</mapper>