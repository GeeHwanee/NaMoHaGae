package kr.kro.namohagae.mall.controller;

//@RequiredArgsConstructor
//@RestController
//@RequestMapping(value = "/api/v1")
public class ProductController_test {
    /*
    private final ProductService productService;
    //private final FavoriteSerice favoriteSerice;


    // rest 방식으로 변경, 찜여부 추가해야함
    @GetMapping(value="/mall/product/list", produces= MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ProductDto.Pagination> list(@RequestParam(defaultValue="1") Integer pageNo, Integer categoryNo, Integer memberNo) {
        ProductDto.Pagination pagination = productService.list(pageNo, categoryNo, memberNo);
        return ResponseEntity.ok(pagination);
    }


    @GetMapping(value="/mall/product/read", produces=MediaType.APPLICATION_JSON_VALUE)
    public ResponseEntity<ProductDto.Read> read(Integer productNo) {
        ProductDto.Read read = productService.read(productNo);
        return ResponseEntity.ok(read);
    }
    */
}
